# Syntalos Package Repository

Publishes packages of Syntalos as native repositories via GitHub.

The repository can be automatically added on Debian and Ubuntu via our small helper script:

```bash
curl -fsSL https://raw.githubusercontent.com/syntalos/repo/refs/heads/main/publish/setup-syntalos-repo.sh | sudo sh
```
